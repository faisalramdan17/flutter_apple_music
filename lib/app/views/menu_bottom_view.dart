import 'package:flutter/material.dart';

class MenuBottomView extends StatelessWidget {
  const MenuBottomView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
