name: kuncie_music
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # GetX is an extra-light and powerful solution for Flutter. It combines high-performance state management, intelligent dependency injection, and route management quickly and practically.
  get: ^4.6.1  # https://pub.dev/packages/get

  # Provides internationalization and localization facilities, including message translation, plurals and genders, date/number formatting and parsing, and bidirectional text.
  intl: ^0.17.0 # https://pub.dev/packages/intl

  # Package for Flutter apps that allows setting the web URL strategy with a single line of code. To remove the leading hash (#) from the URL of your Flutter web app
  url_strategy:  # https://pub.dev/packages/url_strategy

  # A Flutter plugin for launching a URL. Supports iOS, Android, web, Windows, macOS, and Linux.
  url_launcher: ^6.0.18 # https://pub.dev/packages/url_launcher

  # Lottie is a mobile library for Android and iOS that parses Adobe After Effects animations exported as json with Bodymovin and renders them natively on mobile!
  lottie: ^1.2.2  # https://pub.dev/packages/lottie

  # The google_fonts package for Flutter allows you to easily use any of the thousands of fonts available from fonts.google.com in your Flutter app.
  google_fonts: ^2.2.0 # https://pub.dev/packages/google_fonts

  # Iconsax for flutter (1000+ icons 6 diferent styles, total 6000+ icons). the official icons of the Vuesax framework
  iconsax: ^0.0.8 # https://pub.dev/packages/iconsax

  # The Font Awesome Icon pack available as set of Flutter Icons.
  font_awesome_flutter: ^9.2.0 # https://pub.dev/packages/font_awesome_flutter

  # The following adds the Cupertino Icons font to your application. Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2 # https://pub.dev/packages/cupertino_icons

  # A new Flutter dialog with a series of beautiful animations, slide fade rotate size scale rotate3D animations. Dialog barrier include status bar at the top of screen, solved the problem of default dialog.
  flutter_animated_dialog: ^2.0.1 # https://pub.dev/packages/flutter_animated_dialog

  # A collection of loading indicators animated with flutter. Heavily inspired by @tobiasahlin's SpinKit.
  flutter_spinkit: ^5.1.0 # https://pub.dev/packages/flutter_spinkit

  # A fade shimmer library to implement loading like latest facebook loading effect.
  fade_shimmer: ^2.0.1 # https://pub.dev/packages/fade_shimmer

  # A flutter package for creating badges. Badges can be used for an additional marker for any widget, e.g. show a number of items in a shopping cart.
  badges: ^2.0.2 # https://pub.dev/packages/badges

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  flutter_lints: ^1.0.0 # https://pub.dev/packages/flutter_lints

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/icons/
    - assets/lottie/
    - assets/menu/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
